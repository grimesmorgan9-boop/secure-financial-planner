"""
forms package
-------------
Flask-WTF form definitions. Using WTForms gives us server-side input
validation (length limits, type coercion, required fields) and CSRF
tokens on every form "for free", instead of hand-rolling validation
inside route handlers.
"""
